/** @type {import('next').NextConfig} */
const nextConfig = {
  // Para exportar como sitio estático con Next.js App Router
  output: 'export',
  trailingSlash: true,
  // Puedes agregar aquí otras opciones como basePath si lo necesitas
};

module.exports = nextConfig;
